import ProjectDescription

let config = Config(
    plugins: [],
    dependencies: [
        // Tuist Registry（可能需要代理）
        // 如果可以访问 registry.tuist.dev，取消下面的注释
        // .remote(
        //     url: "https://registry.tuist.dev",
        //     name: "Tuist"
        // ),
    ]
)
